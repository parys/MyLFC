'use strict';
angular.module('newsComment.config',
    ['newsComment.factory', 'newsComment.ctrl', 'newsComment.directive'])
    .config([
        '$stateProvider',
        function($stateProvider) {
        }
    ]);