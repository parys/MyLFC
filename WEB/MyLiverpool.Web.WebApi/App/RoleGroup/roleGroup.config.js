'use strict';

angular.module('roleGroup.config',
    ['roleGroup.factory', 'roleGroup.ctrl'])
    .config([
        '$stateProvider',
        function($stateProvider) {
            ;
        }
    ]);