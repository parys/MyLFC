namespace MyLfc.Application.Infrastructure.Extensions;

public static class ValidatorExtensions
{
    //public static IRuleBuilderOptions<T, TGuid>
    //    ShouldExist<T, TGuid, TEntity>(this IRuleBuilder<T, TGuid> ruleBuilder, ILiverpoolContext context) where TEntity : class
    //{
    //    return ruleBuilder.SetValidator(new EntityExistValidator<TEntity>(context));
    //}
}
